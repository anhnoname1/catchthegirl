package com.t3h.catchtheheart.model;

import java.awt.*;

public class Snowflakes extends Item {

    public Snowflakes(Image img, int x, int y, int size, int delay) {
        super( img, x, y, size, delay );
    }
}
