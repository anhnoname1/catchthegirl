package com.t3h.catchtheheart.model;

import java.awt.*;

public class HeartBlack extends Item {
    public HeartBlack(Image img, int x, int y, int size, int delay) {
        super( img, x, y, size, delay );
    }
}
